# === Maintenance mode: show /wartung for EVERYTHING (loop-safe) ===

# Allow maintenance page itself (both forms)
/wartung     /wartung     200
/wartung/*   /wartung     200

# Allow Astro build assets (important, sonst kann es je nach Setup "komisch" wirken)
/_astro/*    /_astro/:splat   200

# Allow static assets used by the maintenance page
/images/*    /images/:splat   200
/favicon*    /favicon:splat   200
/robots.txt  /robots.txt      200
/sitemap*    /sitemap:splat   200

# Everything else: rewrite to maintenance page (NO redirect -> NO loop)
/*           /wartung         200
